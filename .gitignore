# Secrets
.env
*.env
# dot-env VARIANTS too — `*.env` does NOT match `.env.local`,
# `.env.production`, or setup-local.sh's `.env.bak.<timestamp>` backup
# (full token set in plaintext). `.env.example` is a template, keep it.
.env.*
*.env.*
!.env.example
zasder-install-summary.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# SQLite
*.db
*.db-journal
*.db-wal
*.db-shm
data/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# Fly
fly.toml.bak

# Written by the mirror generator as a destination-sanity marker.
.zasder-public-mirror

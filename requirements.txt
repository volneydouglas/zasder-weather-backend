# Bumped 2026-07-11 (security review): FastAPI 0.115.5 pulled Starlette
# 0.41.3, which has the quadratic Range-header DoS (GHSA-7f5h-v6xp-fcq8)
# reachable through the unauthenticated /static mount. 0.139.x pulls
# Starlette >=1.3.1 (fixed); full test suite verified green on it.
fastapi>=0.139,<0.140
uvicorn[standard]==0.32.1
httpx==0.27.2
# Bumped 2026-05-21 — pydantic-core 2.23.x (pinned by pydantic 2.9.x)
# doesn't ship wheels for Python 3.13+. 2.10 series supports 3.13 and
# stays on the v2 API line. Caps below 3 since v3 hasn't shipped yet
# and we'd want to vet it before adopting.
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
aiosqlite==0.20.0
# APNs push: ES256-signed JWT (token auth) + HTTP/2 to api.push.apple.com.
PyJWT[crypto]>=2.8,<3
h2>=4,<5
# MQTT publishing (Home Assistant discovery). Only used when MQTT_HOST is set;
# the publisher imports it lazily, so a stripped install still boots without it.
paho-mqtt>=2,<3
